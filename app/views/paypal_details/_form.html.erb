<%= form_for @paypal_details do |f| %>
  <%= f.hidden_field :user_id, value: @current_user.id %>
  <%= f.label :account_email %>
  <%= f.text_field :account_email %>
  <br>
  <%= f.submit "#{action}" %>
<% end %>